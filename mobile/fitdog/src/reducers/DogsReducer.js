// import {
//   FETCH_DOGS_SUCCESS,
// } from '../actions/types';

// import INITIAL_STATE from '../INITIAL_STATE';
import MOCK_DOGS from '../MOCK_DOGS';

export default function(state = MOCK_DOGS, action) {
  switch (action.type) {
    default:
      return state;
  }
}
